# Telegram Helper Bot

Простой Telegram бот на Python, который отправляет приветственное сообщение с ID пользователя при команде `/start`.

## Установка и настройка

### 🐳 Запуск через Docker (рекомендуется)

1. **Создайте бота в Telegram:**
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте команду `/newbot`
   - Следуйте инструкциям для создания бота
   - Скопируйте полученный токен

2. **Настройте переменные окружения:**
   - Откройте файл `.env`
   - Замените `your_bot_token_here` на токен вашего бота:
   ```
   BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   DB_PATH=/app/x-ui.db
   ```

3. **Убедитесь, что файл БД существует:**
   - Поместите файл `x-ui.db` в корень проекта

4. **Запустите бота:**
   ```bash
   docker-compose up -d
   ```

5. **Просмотр логов:**
   ```bash
   docker-compose logs -f telegram-bot
   ```

6. **Остановка бота:**
   ```bash
   docker-compose down
   ```

### 🐍 Запуск локально (Python)

1. **Клонируйте репозиторий или скачайте файлы**

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Создайте бота в Telegram:**
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте команду `/newbot`
   - Следуйте инструкциям для создания бота
   - Скопируйте полученный токен

4. **Настройте переменные окружения:**
   - Откройте файл `.env`
   - Замените `your_bot_token_here` на токен вашего бота:
   ```
   BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

5. **Запустите бота:**
   ```bash
   python bot.py
   ```

## Использование

После запуска бота отправьте ему команду `/start` в Telegram. Бот ответит приветственным сообщением с вашим Telegram ID.

## Структура проекта

```
3xui-helper-bot/
├── bot.py              # Основной файл бота
├── database.py         # Модуль для работы с БД
├── requirements.txt    # Зависимости Python
├── .env                # Переменные окружения (секретные данные)
├── x-ui.db            # База данных SQLite
├── Dockerfile         # Docker образ
├── docker-compose.yml # Docker Compose конфигурация
├── .dockerignore      # Исключения для Docker
├── testdbchange.py    # Тестовый скрипт для проверки мониторинга
└── README.md          # Документация
```

## Безопасность

- Никогда не коммитьте файл `.env` в репозиторий
- Добавьте `.env` в `.gitignore` если используете Git
- Храните токен бота в безопасности
